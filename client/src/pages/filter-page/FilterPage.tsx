import { FC } from "react";
import "./filterPage.style.scss";

const FilterPage: FC = () => {
  return <section className="filter-page content-page">FilterPage</section>;
};

export default FilterPage;
