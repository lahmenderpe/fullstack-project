import { AppContextStateType } from "../@types/context/AppContextTypes";
import { ReducerType } from "../@types/commonTypes";

const reducer = (
  state: AppContextStateType,
  action: ReducerType<AppContextStateType>
) => {
  return state;
};

export default reducer;
